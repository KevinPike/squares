{
  "name": "squares",
  "version": "1.0.0",
  "description": "Canvas-based geometric visualization",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.2.0",
    "jsdom": "^23.2.0"
  }
}
